{
  "tables": [
    {
      "name": "authors",
      "columns": [
        { "name": "id", "type": "auto-number", "nullable": false },
        { "name": "name", "type": "text", "nullable": false, "maxLength": 120 },
        { "name": "name", "type": "text", "nullable": true, "maxLength": 60 }
      ],
      "primaryKey": ["id"]
    },
    {
      "name": "books",
      "columns": [
        { "name": "id", "type": "auto-number", "nullable": false },
        { "name": "title", "type": "text", "nullable": false, "maxLength": 200 },
        { "name": "author_name", "type": "text", "nullable": false, "maxLength": 120 },
        { "name": "shelf_code", "type": "text", "nullable": false, "maxLength": 10 }
      ],
      "primaryKey": ["id", "isbn"],
      "foreignKeys": [
        { "column": "author_name", "references": { "table": "authors", "column": "name" } },
        { "column": "shelf_code", "references": { "table": "shelves", "column": "code" } }
      ]
    },
    {
      "name": "loans",
      "columns": [
        { "name": "book_id", "type": "unique-id", "nullable": false },
        { "name": "borrowed_at", "type": "date-time", "nullable": true }
      ],
      "primaryKey": ["book_id", "borrowed_at"],
      "foreignKeys": [
        { "column": "book_id", "references": { "table": "books", "column": "id" } }
      ]
    },
    {
      "name": "loans",
      "columns": [
        { "name": "id", "type": "auto-number", "nullable": false }
      ],
      "primaryKey": ["id"]
    }
  ]
}
